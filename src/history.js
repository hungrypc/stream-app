// we are creating our own history object so entire app can access this easier

import { createBrowserHistory } from 'history'; //react-router-dom installed this 
export default createBrowserHistory();